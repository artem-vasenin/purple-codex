# Промты и затраты токенов

## Бэкенд

### 3331 948 578 -> 1 944 532 -> ? -> 1 935 402

- /plan
Необходимо реализовать на бэкэнде возможнось регистрации пользователя, входа пользователя (пока без восстановления пароля), который получает в результате JWT токен и refresh token.
Также необходима механика обновления jwt токена по refresh токену

### 1 935 402 -> 1 927 291

- /default mode
Сохрани этот план в md файле в папке ./docs

### 1 927 291 -> 1 918 542

- /default mode
Теперь в этот план который ты сохранил в md добавь отдельную секцию - задачи, и там декомпозируй все задачи, которые необходимо сделать для имплементации.
Это планы с чекбоксами. Далее по ходу реализации ты будешь эти чекбоксы отмечать как выполненные

### 1 918 542 -> 1 818 950 (уау слопало 100 000 токенов!)

- /default mode
Приступай к реализации плана и отмечай выполненные пункты

## Фронтенд

### 1 818 950 -> ? -> 1 795 181

- /plan
Теперь твоя задача реализовать фронтенд авторизации для того, чтобы пользователь мог создать аккаунт, входить получая нужные токены и сохраняя их себе в
HTTP cookies only как и планировали.

### 1 795 181 -> 1 726 259

Yes implement this plan

### 1 726 259 -> 1 708 535

- /default mode
Создай скрипт запуска фронтенда и бэкенда одновременно

### 1 708 535 -> 1 673 587

- /default mode
Примени миграции

```bash
migrate -path backend/migrations \
-database "postgres://postgres:postgres@localhost:5433/app?sslmode=disable" \
up
```
